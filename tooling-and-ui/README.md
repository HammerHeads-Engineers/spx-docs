# Tooling & UI

Centralize everything related to day-to-day tooling:

- Explore the SPX web interface to monitor simulations and tweak parameters.
- Connect your IDE (for example, VS Code) to a running SPX Server container for rapid iteration.

Use the subsections to jump into the specific guide you need.
